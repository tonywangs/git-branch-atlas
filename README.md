# Git Branch Atlas

A terminal tool for exploring Git commit and branch history.

Initial development is in progress. The first release will provide a useful
terminal graph with a documented command, examples, and tests against real Git
repositories. No runtime dependencies beyond Python and Git are intended.
